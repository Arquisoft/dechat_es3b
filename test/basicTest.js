require("chai");
var assert = require("assert");
describe("Test chat", function () {
  it("2 is 2", function () {
    assert.equal(2, 2);
  });
  it("3+3 is 6", function () {
    assert.equal(3+3, 6);
  });
    

});